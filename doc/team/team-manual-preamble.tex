\documentclass[11pt,a4paper,titlepage]{article}

% $Id$

% For up-to-date definitions of runlimits, etc.
\input{systemconfig}

\usepackage{a4wide}
\usepackage{moreverb}
\usepackage{graphicx}
\usepackage{expdlist}
\usepackage{svn}

% Begin new paragraphs without indentation but vertical space.
\setlength{\parindent}{0pt}
\setlength{\parskip}{1.5ex plus 0.5ex minus 0.2ex}

\newcommand{\DOMjudge}{\textsc{DOM}judge }

% We define our own \url command, because the one in the url package
% doesn't expand defines (present in systemconfig.tex).
\newcommand{\url}[1]{\texttt{#1}}

% Display commands, arguments, etc. in texttt font and don't break those.
\newcommand{\cmd}[1]{\mbox{\texttt{#1}}}

% Our titlepage
\makeatletter
\def\maketitle{%
  \null
  \thispagestyle{empty}%
  \begin{figure}[htbp]
    \centerline{\includegraphics[height=5cm]{ICPClogo-normal}}
  \end{figure}
  \vskip 1cm
  \begin{center}\leavevmode
    \normalfont
    {\fontsize{30pt}{30pt}\selectfont \@title \par}%
    \vskip 1cm
    {\Large \@author \par}%
    \vskip 1cm
    {\Large \@date \par}%
  \end{center}%
  \vskip 1cm
  \begin{figure}[htbp]
    \centerline{\includegraphics[height=6cm]{../logos/DOMjudgelogo.pdf}}
  \end{figure}
  \vfill\null
}
\makeatother
