# Format: '<role>:<db_host>:<db_name>:<user>:<password>'
# Update the following line with the name of the database
# server instead of localhost, and the password of
# the domjudge_jury user:
jury:localhost:domjudge:domjudge_jury:thepassword
