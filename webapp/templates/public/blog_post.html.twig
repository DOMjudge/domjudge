{% extends "public/base.html.twig" %}

{% block title %}Začátek našeho programátorského dobrodružství!{% endblock %}
{% block content %}
    <div class="container">
        <h1>{{ title }}</h1>
        <h4 class="text-left text-muted">{{ subtitle }}</h4>
        <p>{{ publishTime|round|date }}</p>

        <div>
            {{ body|raw }}
            <h5 class="text-muted">{{ author }}</h5>
        </div>

        <a href="/public/blog" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Zpět ke všem článkům</a>
    </div>
{% endblock %}
