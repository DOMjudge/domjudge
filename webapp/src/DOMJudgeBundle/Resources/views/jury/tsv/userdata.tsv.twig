userdata	1
{% for row in data %}
{{ row.type|replace({'\t':' '}) }}	{{ row.id|replace({'\t':' '}) }}	{{ row.fullname|replace({'\t':' '}) }}	{{ row.username|replace({'\t':' '}) }}	{{ row.password|replace({'\t':' '}) }}
{% endfor %}
