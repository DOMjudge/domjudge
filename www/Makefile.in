install-domserver:
	install -d @domserverwww_path@
	cp -a *.js *.css *.php images @domserverwww_path@
	cp -a public team jury @domserverwww_path@

clean:
	rm -f Makefile configure.php
	ln -s ../run-configure Makefile
	-rm -f `find $(PWD) -name \*~`

distclean:
	rm -f Makefile

