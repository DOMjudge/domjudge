INT(1,100,runs) NEWLINE
REP(runs)
  IF(MATCH("i"))
    STRING("impossible") NEWLINE
  ELSE
    INT(0,100,n)
    REP(n)
      SPACE
      STRING("(")
      INT(-100,100,x)
      STRING(",")
      INT(-100,100,y)
      STRING(")")
    END
    NEWLINE
  END
END
EOF
